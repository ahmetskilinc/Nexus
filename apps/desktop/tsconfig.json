{
  "// note": "Editor/CLI-facing config (the shadcn CLI resolves aliases from tsconfig.json). Typechecking uses tsconfig.main.json + tsconfig.renderer.json via `bun run typecheck`.",
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": { "@/*": ["src/renderer/*"] }
  },
  "include": ["src"]
}
